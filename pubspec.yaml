name: vokabeltrainer
description: "Moderner Vokabeltrainer für Android, iOS und Web."
publish_to: "none"

version: 1.1.0+2

environment:
  sdk: ">=3.11.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_local_notifications: ^22.2.0
  flutter_timezone: ^5.1.0
  timezone: ^0.11.1
  shared_preferences: ^2.5.5
  http: ^1.5.0
  path_provider: ^2.1.5
  fl_chart: ^1.2.0
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
